/**
 * System · Storage rotası — UI-ADR-200.
 * Ekran gövdesi `features/system/storage/screen.tsx`.
 */

import { SystemStorage } from "@/features/system/storage/screen";

export default function SystemStoragePage() {
  return <SystemStorage />;
}
