/**
 * System · Version rotası — UI-ADR-211.
 * Ekran gövdesi `features/system/version/screen.tsx`.
 */

import { SystemVersion } from "@/features/system/version/screen";

export default function SystemVersionPage() {
  return <SystemVersion />;
}
