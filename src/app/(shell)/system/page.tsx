/**
 * System Director rotası — 06-workspaces.md §8.
 * Ekran gövdesi `features/system/screen.tsx`.
 */

import { SystemDirector } from "@/features/system/screen";

export default function SystemPage() {
  return <SystemDirector />;
}
