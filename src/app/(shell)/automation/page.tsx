/**
 * Automation rotası — UI-ADR-210.
 * Ekran gövdesi `features/automation/screen.tsx`.
 */

import { Automation } from "@/features/automation/screen";

export default function AutomationPage() {
  return <Automation />;
}
