/**
 * Executive Council rotası — 06-workspaces.md §5.
 * Ekran gövdesi `features/council/screen.tsx`.
 */

import { ExecutiveCouncil } from "@/features/council/screen";

export default function CouncilPage() {
  return <ExecutiveCouncil />;
}
