/**
 * Memory rotası — UI-ADR-201.
 * Ekran gövdesi `features/memory/screen.tsx`.
 */

import { Memory } from "@/features/memory/screen";

export default function MemoryPage() {
  return <Memory />;
}
