/**
 * Amazon · Inventory rotası — UI-ADR-196.
 * Ekran gövdesi `features/amazon/inventory/screen.tsx`.
 */

import { AmazonInventory } from "@/features/amazon/inventory/screen";

export default function AmazonInventoryPage() {
  return <AmazonInventory />;
}
