/**
 * Amazon · Listings rotası — UI-ADR-203.
 * Ekran gövdesi `features/amazon/listings/screen.tsx`.
 */

import { AmazonListings } from "@/features/amazon/listings/screen";

export default function AmazonListingsPage() {
  return <AmazonListings />;
}
