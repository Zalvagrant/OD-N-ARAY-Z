/**
 * Amazon · PPC rotası — UI-ADR-197.
 * Ekran gövdesi `features/amazon/ppc/screen.tsx`.
 */

import { AmazonPpc } from "@/features/amazon/ppc/screen";

export default function AmazonPpcPage() {
  return <AmazonPpc />;
}
