/**
 * Amazon · Returns rotası — UI-ADR-203.
 * Ekran gövdesi `features/amazon/returns/screen.tsx`.
 */

import { AmazonReturns } from "@/features/amazon/returns/screen";

export default function AmazonReturnsPage() {
  return <AmazonReturns />;
}
