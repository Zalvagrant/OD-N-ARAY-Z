/**
 * Amazon · Orders rotası — UI-ADR-202.
 * Ekran gövdesi `features/amazon/orders/screen.tsx`.
 */

import { AmazonOrders } from "@/features/amazon/orders/screen";

export default function AmazonOrdersPage() {
  return <AmazonOrders />;
}
