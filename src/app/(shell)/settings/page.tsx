/**
 * Settings rotası — UI-ADR-211.
 * Ekran gövdesi `features/settings/screen.tsx`.
 */

import { Settings } from "@/features/settings/screen";

export default function SettingsPage() {
  return <Settings />;
}
