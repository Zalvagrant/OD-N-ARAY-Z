/**
 * Knowledge Workspace rotası — 06-workspaces.md §3.
 * Ekran gövdesi `features/knowledge/screen.tsx`.
 */

import { KnowledgeWorkspace } from "@/features/knowledge/screen";

export default function KnowledgePage() {
  return <KnowledgeWorkspace />;
}
