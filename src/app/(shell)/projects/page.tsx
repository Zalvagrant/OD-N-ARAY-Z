/**
 * Projects rotası — UI-ADR-208.
 * Ekran gövdesi `features/projects/screen.tsx`.
 */

import { Projects } from "@/features/projects/screen";

export default function ProjectsPage() {
  return <Projects />;
}
