/**
 * Decision Center rotası — 06-workspaces.md §2.
 * Ekran gövdesi `features/decisions/screen.tsx`.
 */

import { DecisionCenter } from "@/features/decisions/screen";

export default function DecisionsPage() {
  return <DecisionCenter />;
}
