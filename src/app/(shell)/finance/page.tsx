/**
 * Finance rotası — UI-ADR-195.
 * Ekran gövdesi `features/finance/screen.tsx`.
 */

import { Finance } from "@/features/finance/screen";

export default function FinancePage() {
  return <Finance />;
}
